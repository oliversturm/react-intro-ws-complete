import ProjectOverview from './ProjectOverview';
import styles from './index.module.css';

const OptimizationFinalPage = () => {
  return (
    <div className={styles.outer}>
      <ProjectOverview />
    </div>
  );
};

export default OptimizationFinalPage;
